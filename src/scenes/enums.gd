class_name Enums extends RefCounted

enum MemoryCell { 
	WHITE, 
	RED,
	GREEN
}

enum DamageSource {
	BULLET_RED,
	BULLET_WHITE,
	VIRUS_RED,
	VIRUS_WHITE
}

enum Mutation {
	WHITE,
	RED,
	BLUE,
	GREEN
}

enum BulletPattern {
	SINGLE,
	BURST
}
